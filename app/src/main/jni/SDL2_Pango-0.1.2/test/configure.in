AC_INIT(testbench.c)

AC_CANONICAL_HOST
AC_CANONICAL_TARGET

SDLPANGO_VERSION=0.1.1

AM_INIT_AUTOMAKE(SDLPango_test, $SDLPANGO_VERSION)

AC_PROG_CC

AC_C_CONST

SDL_VERSION=1.2.4

AM_PATH_SDL($SDL_VERSION,
            :,
            AC_MSG_ERROR([*** SDL version $SDL_VERSION not found!])
)

CFLAGS="$CFLAGS $SDL_CFLAGS"
LIBS="$LIBS $SDL_LIBS"

PKG_CHECK_MODULES(SDLPANGO, SDL_Pango >= $SDLPANGO_VERSION, , AC_MSG_ERROR([*** SDL_Pango >= $SDLPANGO_VERSION not found!]))
CFLAGS="$CFLAGS $SDLPANGO_CFLAGS"
LIBS="$LIBS $SDLPANGO_LIBS"

AC_CONFIG_FILES(Makefile)

# Finally create all the generated files
AC_OUTPUT
